

export const enum ResourceEventType {
  /**
   * The data of the resource has changed.
   */
  DataChange,

  /**
   * A command has been executed, be it successful or not.
   */
  CommandExecuted,
}
